"use client";
import { useEffect, useRef, useState } from "react";
import { Bot, FileText, Send, Sparkles, User } from "lucide-react";
import { useApp } from "@/components/providers/app-provider";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Select } from "@/components/ui/field";

const prompts = ["What should I work on today?", "Why is this project at risk?", "What is overdue?", "What is still missing for submission?"];
export default function AssistantPage() {
  const { projects, activeProject, messages, sendMessage } = useApp(); const [projectId, setProjectId] = useState(activeProject?.id ?? ""); const [input, setInput] = useState(""); const end = useRef<HTMLDivElement>(null); useEffect(() => end.current?.scrollIntoView({ behavior: "smooth" }), [messages]);
  function send(content = input) { if (!content.trim()) return; sendMessage(content.trim(), projectId); setInput(""); }
  return <div className="mx-auto max-w-5xl space-y-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-sky-700">Shared AI workspace</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Ask Skyler</h1><p className="mt-2 text-slate-500">Advice is conversational. Any database change appears as a proposal first.</p></div><Select aria-label="Project context" className="max-w-xs" value={projectId} onChange={(event) => setProjectId(event.target.value)}>{projects.filter((project) => project.status !== "archived").map((project) => <option key={project.id} value={project.id}>{project.title}</option>)}</Select></div><div className="flex flex-wrap gap-2">{prompts.map((prompt) => <button key={prompt} onClick={() => send(prompt)} className="rounded-full border border-slate-200 bg-white px-3 py-2 text-xs font-medium text-slate-600 hover:border-sky-300 hover:text-sky-700">{prompt}</button>)}</div><Card className="overflow-hidden"><div className="flex h-[620px] flex-col"><div className="flex items-center gap-3 border-b border-slate-100 px-5 py-4"><div className="grid h-9 w-9 place-items-center rounded-xl bg-slate-950 text-white"><Bot className="h-4 w-4" /></div><div><p className="text-sm font-semibold">Skyler</p><p className="text-xs text-emerald-600">Project retrieval ready</p></div><div className="ml-auto flex gap-2"><Badge tone="info">Project scoped</Badge><Badge tone="success">Actions gated</Badge></div></div><div className="scrollbar-thin flex-1 space-y-5 overflow-y-auto bg-slate-50/50 p-5">{messages.map((message) => <div key={message.id} className={`flex gap-3 ${message.role === "user" ? "justify-end" : "justify-start"}`}>{message.role === "assistant" && <div className="grid h-8 w-8 shrink-0 place-items-center rounded-xl bg-slate-950 text-white"><Sparkles className="h-3.5 w-3.5" /></div>}<div className={`max-w-[82%] rounded-2xl px-4 py-3 text-sm leading-6 ${message.role === "user" ? "bg-sky-600 text-white" : "border border-slate-200 bg-white text-slate-700 shadow-sm"}`}><p>{message.content}</p>{message.citations && <div className="mt-3 border-t border-slate-100 pt-3"><p className="mb-2 text-[10px] font-semibold uppercase tracking-wider text-slate-400">Sources</p>{message.citations.map((citation) => <span key={`${citation.filename}-${citation.reference}`} className="mr-2 inline-flex items-center gap-1 rounded-lg bg-slate-100 px-2 py-1 text-xs text-slate-600"><FileText className="h-3 w-3" />{citation.filename} · {citation.reference}</span>)}</div>}</div>{message.role === "user" && <div className="grid h-8 w-8 shrink-0 place-items-center rounded-xl bg-sky-100 text-sky-700"><User className="h-3.5 w-3.5" /></div>}</div>)}<div ref={end} /></div><form onSubmit={(event) => { event.preventDefault(); send(); }} className="flex gap-2 border-t border-slate-100 bg-white p-4"><textarea aria-label="Message Skyler" rows={1} value={input} onChange={(event) => setInput(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); send(); } }} placeholder="Ask about this project’s work, evidence, or risk…" className="max-h-32 min-h-11 flex-1 resize-none rounded-xl border border-slate-200 px-3.5 py-3 text-sm outline-none focus:border-sky-400 focus:ring-4 focus:ring-sky-100" /><Button type="submit" size="icon" disabled={!input.trim()} aria-label="Send message"><Send className="h-4 w-4" /></Button></form></div></Card></div>;
}
