"use client";
import { useState } from "react";
import { Bot, CheckCircle2, Clock3, Database, Eye, EyeOff, KeyRound, Send, ShieldCheck, Smartphone, Webhook } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { Input, Label, Select } from "@/components/ui/field";

export default function SettingsPage() { const [mode,setMode]=useState("full_plan"); const [show,setShow]=useState(false); const [tested,setTested]=useState<string[]>([]); function test(name:string){setTested((current)=>[...new Set([...current,name])]);} return <div className="space-y-6"><div><p className="text-sm font-semibold text-sky-700">Preferences & connections</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Settings</h1><p className="mt-2 text-slate-500">Non-secret preferences live here. Credentials remain in server environment variables.</p></div><div className="grid gap-6 xl:grid-cols-2"><Card><CardHeader><div><h2 className="text-lg font-semibold">Planning preferences</h2><p className="mt-1 text-sm text-slate-500">Used by deterministic daily planning and reminders.</p></div><Clock3 className="h-5 w-5 text-slate-400" /></CardHeader><CardContent className="grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><Label htmlFor="timezone">Timezone</Label><Input id="timezone" value="Asia/Kuala_Lumpur" readOnly /></div><div><Label htmlFor="morning">Morning reminder</Label><Input id="morning" type="time" defaultValue="08:00" /></div><div><Label htmlFor="evening">Evening reminder</Label><Input id="evening" type="time" defaultValue="20:00" /></div><div><Label htmlFor="capacity">Daily work limit</Label><Input id="capacity" type="number" min="1" max="24" step="0.5" defaultValue="6" /></div><div><Label htmlFor="duration">Default task duration</Label><Select id="duration" className="w-full" defaultValue="60"><option value="30">30 minutes</option><option value="60">1 hour</option><option value="90">1.5 hours</option><option value="120">2 hours</option></Select></div><Button className="sm:col-span-2">Save preferences</Button></CardContent></Card><Card><CardHeader><div><h2 className="text-lg font-semibold">Plan approval mode</h2><p className="mt-1 text-sm text-slate-500">Skyler always asks before applying generated plans or date changes.</p></div><ShieldCheck className="h-5 w-5 text-slate-400" /></CardHeader><CardContent className="space-y-3"><button onClick={()=>setMode("full_plan")} className={`w-full rounded-2xl border p-4 text-left ${mode==="full_plan"?"border-sky-400 bg-sky-50":"border-slate-200"}`}><div className="flex items-center justify-between"><p className="font-semibold">Approve entire plan</p>{mode==="full_plan"&&<CheckCircle2 className="h-5 w-5 text-sky-600" />}</div><p className="mt-1 text-sm text-slate-500">Review everything, edit if needed, then approve in one transaction.</p></button><button onClick={()=>setMode("milestone_by_milestone")} className={`w-full rounded-2xl border p-4 text-left ${mode==="milestone_by_milestone"?"border-sky-400 bg-sky-50":"border-slate-200"}`}><div className="flex items-center justify-between"><p className="font-semibold">Milestone by milestone</p>{mode==="milestone_by_milestone"&&<CheckCircle2 className="h-5 w-5 text-sky-600" />}</div><p className="mt-1 text-sm text-slate-500">Approve milestones and individual tasks gradually; resume later.</p></button></CardContent></Card></div><Card><CardHeader><div><h2 className="text-lg font-semibold">Service connections</h2><p className="mt-1 text-sm text-slate-500">Status checks never reveal raw secrets.</p></div><Badge tone="warning">Demo mode</Badge></CardHeader><CardContent><div className="grid gap-3 md:grid-cols-3">{[{name:"Supabase",icon:Database,detail:"Database + private storage"},{name:"Xiaomi MiMo",icon:Bot,detail:"Structured planning + chat"},{name:"Telegram",icon:Send,detail:"Webhook + daily workflows"}].map((service)=><div key={service.name} className="rounded-2xl border border-slate-200 p-4"><div className="flex items-center justify-between"><div className="grid h-10 w-10 place-items-center rounded-xl bg-slate-100"><service.icon className="h-5 w-5" /></div><Badge tone={tested.includes(service.name)?"success":"neutral"}>{tested.includes(service.name)?"Mock verified":"Not connected"}</Badge></div><p className="mt-4 font-semibold">{service.name}</p><p className="mt-1 text-sm text-slate-500">{service.detail}</p><Button variant="secondary" size="sm" className="mt-4 w-full" onClick={()=>test(service.name)}>Test connection</Button></div>)}</div></CardContent></Card><div className="grid gap-6 xl:grid-cols-2"><Card><CardHeader><div><h2 className="text-lg font-semibold">Telegram security</h2><p className="mt-1 text-sm text-slate-500">Only one configured chat ID is accepted.</p></div><Smartphone className="h-5 w-5 text-slate-400" /></CardHeader><CardContent className="space-y-3"><Row icon={ShieldCheck} label="Allowed chat ID" value="Configured in environment" /><Row icon={Webhook} label="Webhook secret" value="Required and hidden" /><Row icon={CheckCircle2} label="Duplicate protection" value="Enabled" /><Button variant="secondary" className="w-full">View webhook status</Button></CardContent></Card><Card><CardHeader><div><h2 className="text-lg font-semibold">Change password</h2><p className="mt-1 text-sm text-slate-500">Creates a new Argon2 hash; plaintext is never stored.</p></div><KeyRound className="h-5 w-5 text-slate-400" /></CardHeader><CardContent className="space-y-4"><div><Label htmlFor="current-password">Current password</Label><div className="relative"><Input id="current-password" type={show?"text":"password"} className="pr-12" /><button aria-label={show?"Hide password":"Show password"} onClick={()=>setShow(!show)} className="absolute right-2 top-2.5 text-slate-400">{show?<EyeOff className="h-5 w-5" />:<Eye className="h-5 w-5" />}</button></div></div><div><Label htmlFor="new-password">New password</Label><Input id="new-password" type="password" minLength={10} /></div><Button className="w-full">Generate replacement hash</Button><p className="text-xs leading-5 text-slate-400">You will copy the generated hash into Vercel and restart the deployment. The app never stores the new plaintext password.</p></CardContent></Card></div></div>; }
function Row({icon:Icon,label,value}:{icon:typeof ShieldCheck;label:string;value:string}){return <div className="flex items-center gap-3 rounded-xl bg-slate-50 p-3"><Icon className="h-4 w-4 text-sky-600" /><div><p className="text-sm font-medium">{label}</p><p className="text-xs text-slate-400">{value}</p></div></div>}

